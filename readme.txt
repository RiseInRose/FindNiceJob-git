启动方式：依次启动main1，main2，main3

爬虫获得工作岗位，并且在地图上表示。

自动规划线路

符合条件职位自动投递简历。
关键词过滤。

能够检查投递简历历史，能够记录链接。
使用Django后台，有登录验证。有多用户操作。
加入平均薪资查询。

模块：
百度地图Python。
51job爬虫（分布式？）
地图标记

多线程模式
1。采用多线程
2。支持断点续爬。

代理模块：
1。自动获取需要代理服务器，然后预备测试将要访问的域名。
2。

会员模块，有人注册，每个邮箱只能注册一个账号，每个人只能搜索3个工作。
如果找到合适的工作，公众号会发送消息。

先设计2层模版的，先满足功能，后面美化！

1。需要设计同步跟新数据，有一点数据，就往数据库添加一点。（2个main同时工作，一个提取数据，一个更新数据。）
目前更新数据有些傻，只能全部更新。后面优化，只更新需要的部分。

2。需要设计ajax反馈。实时向用户展示状态。